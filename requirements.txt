# FastAPI and server
fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.9

# Database - Firestore
firebase-admin==6.4.0

# Database - Legacy (keep for migration)
# sqlalchemy==2.0.29
# psycopg2-binary==2.9.9

# Cache and message broker
redis==5.0.3

# Environment
python-dotenv==1.0.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# LangChain core
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.0.50

# LLM Providers (all free tiers — llm_provider.py handles fallback)
langchain-groq>=0.1.6          # Provider 1: Groq (LLaMA 3.3 70B) — primary
groq>=0.9.0
langchain-openai>=0.1.0        # Provider 2: OpenRouter + Provider 3: NVIDIA NIM (OpenAI-compatible)
openai>=1.14.0
langchain-google-genai>=1.0.0  # Provider 4: Google Gemini 2.0 Flash
langchain-cohere>=0.1.0        # Provider 5: Cohere command-r

# Background tasks
celery==5.3.6
kombu==5.3.5

# Data processing
numpy==1.26.4
pydantic==2.6.4
pydantic-settings==2.2.1

# Email (for notifications)
emails==0.6

# HTTP client
httpx==0.27.0

# HTML parsing (for LinkedIn scraper)
beautifulsoup4==4.12.3
lxml==5.2.2

# Azure Blob Storage (for media files)
azure-storage-blob==12.19.0
azure-identity==1.15.0

# File parsing (PDF, DOCX)
PyPDF2==3.0.1
python-docx==1.1.0
pdf2image==1.17.0
Pillow==10.2.0
pypdf==4.0.1
